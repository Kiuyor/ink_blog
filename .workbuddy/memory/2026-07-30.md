# 2026-07-30

## ink_blog 改进计划启动（improve.md P0–P3）

用户确认按「全部 P0–P3 排期」执行改进计划（计划模式）。今日完成 **P0 全部子任务**：

- **P0-1 rebrand**：`src/config.ts` 改为 title=`ink_blog`、subtitle=`磨叽的墨迹`、lang=`zh_CN`、themeColor.hue=260（由 #9370DB 换算）、profile.name=`ink`、bio=`嗨喽，我是墨水`、GitHub 链接→`https://github.com/Kiuyor`；`astro.config.mjs` 的 site→`https://blog.suchitems.top/`。头像保留 `demo-avatar.png`（用户说"使用现在的"，但提醒仍是 Fuwari 示例图，可后续生成紫色「墨」风格头像）。
- **P0-2 内容**：删除 6 篇 Fuwari 示例文；新建 4 篇占位文：`why-i-write.md`、`hello-im-ink.md`、`markdown-showcase.md`（展示富 Markdown）、`my-toolchain.md`。
- **P0-3 中文**：lang=zh_CN 已生效（产物 `<html lang="zh-CN">`）；构建通过，Pagefind 索引 5 篇中文页/402 词，识别 zh-cn。⚠️ 局限：Pagefind 对 zh-cn 无 stemming/分词，中文短语搜索质量有限，建议加纯 JS 分词预处理（如 segmentit）作为增强项。
- **P0-4 About**：`src/content/spec/about.md` 重写为「数据归你/无算法/无订阅/可迁移」差异化卖点。

## 环境修复（重要 gotcha）

- pnpm 自身损坏（`Failed to switch pnpm to v9.14.4`，CLI 缺失）；用 `npm i -g pnpm@9.14.4` 修复成功。
- `node_modules` 安装不完整（仅 42 个包，astro 缺失）；解决：`rm -rf node_modules && pnpm install --reporter=append-only`（耗时 ~1m23s，成功）。
- 构建命令：`pnpm build`（= `astro build && pagefind --site dist`）。

## 已确认的产品决策

- 隐私分析：**Umami 自托管**（与「数据归你」人设一致）。
- Giscus 评论仓库：`https://github.com/Kiuyor/ink_blog`（需公开仓库，部署时接入）。
- 用户昵称在 USER.md 为「米汤」，但博客作者 persona 为「ink / 墨水」（两者不同，勿混淆）。

## P0 收尾增强（用户确认的两点）

用户确认：(a) 现在就做中文分词；(b) 生成专属紫色头像。两项均已完成。

### 中文分词（构建期注入隐藏分词镜像）
- 新增 `scripts/segment-zh.mjs`，在 `astro build` 之后、`pagefind` 之前运行。
- 实现：用 cheerio **只读**提取每个 `data-pagefind-body` 文本 → `Intl.Segmenter('zh-CN',{granularity:'word'})` 做词级分词（Node 22 内置 ICU，无需额外依赖）→ 在 `</body>` 前注入一个 `display:none` 的隐藏 `data-pagefind-body`（class `pagefind-zh-seg`），内容为空格分词后的中文，供 Pagefind 按词建索引；可见文本保持干净。
- 关键坑：cheerio 全文档 `$.html()` 重新序列化会破坏产物（rebrand 串丢失、多余改动）→ 改为「cheerio 只读取文本 + 字符串 replace 注入」，不再重序列化整页。
- 另一坑：早期手写平衡标签扫描遇到不平衡 `<div>` 会提前截断正文；`segmentText` 曾误加 `/^[一-鿿]/` 过滤器把中文词全丢掉 → 已删除，仅保留「纯标点/符号」过滤。
- `package.json` build 改为：`astro build && node scripts/segment-zh.mjs && pagefind --site dist`。
- 验证：5 个索引页均有隐藏镜像；分词示例 `为什么 写 这个 博客 本文 为 占位 草稿`；Pagefind 索引 5 页/480 词/zh-cn。

### 专属头像（紫色 #9370DB「墨」风格）
- 新增 `scripts/gen-avatar.mjs`：手写 SVG（墨滴 + 书法笔触 + 飞溅墨点，紫色渐变 #b794f6→#9370DB→#6d28d9，透明背景）→ 用 sharp 栅格化为 `src/assets/images/demo-avatar.png`（512×512，透明）。`config.ts` 的 avatar 路径无需改动即生效。
- ⚠️ ImageGen 两次均报 TLS 连接错误（`Client network socket disconnected before secure TLS connection was established`），AI 生成不可用；改用手写 SVG 兜底，效果可控、品牌一致。若用户后续要 AI 版可重试。

### 头像更新（22:38）
- 用户提供了真实头像 PNG：`C:\Users\75017\Desktop\d2764ff4-9f72-40bb-b9a8-36026443ed71.png`（1254×1254 正方形 RGB），`cp -f` 覆盖到 `src/assets/images/demo-avatar.png`（仅读取 Desktop、写入项目，未删 Desktop 原文件）。
- `pnpm build` 重新走 Astro 图片管线，产出 `dist/_astro/demo-avatar.*.webp`（208KB，原 2.3MB PNG 已优化），全站页（header / about / 各文章）均引用。
- 注意：若重新跑 `gen-avatar.mjs` 会覆盖回 SVG 兜底图；该脚本已非当前头像来源。

### 头像文件重命名（22:46）
- `src/assets/images/demo-avatar.png` → `src/assets/images/ink-avatar.png`（语义化，贴合 ink 品牌）。
- `src/config.ts` 的 `avatar` 改为 `assets/images/ink-avatar.png`；`scripts/gen-avatar.mjs` 输出路径同步改为 `ink-avatar.png`。
- `pnpm build` 验证：产物 `dist/_astro/ink-avatar.*.webp`（208KB），全站引用无 `demo-avatar` 残留。

## P1 #5 子②：llms.txt + llms-full.txt（GEO 机器可读索引，用户「推进p1」）

- 新增 `src/utils/llms-txt.ts`：两个纯函数 `buildLlmsTxt(siteBase)` / `buildLlmsFullTxt(siteBase)`，从 `getSortedPosts()` 聚合（标题/描述/slug/body），URL 用 `context.site` 拼成绝对地址。
- 新增端点 `src/pages/llms.txt.ts` 与 `src/pages/llms-full.txt.ts`：静态构建产出 `dist/llms.txt`（索引，951B）与 `dist/llms-full.txt`（全文，169 行）。Content-Type `text/plain`。
- 格式遵循 llmstxt.org：`# 站点` + `> 副标题` + `## Posts`（每项 `- [标题](绝对URL): 描述`）+ `## Other`（About/Archive/RSS）。
- 验证：`pnpm build` 通过；`/llms.txt`、`/llms-full.txt` 均 HTTP 200，URL 为 `https://blog.suchitems.top/...`。
- ⚠️ 仅做得到、无外部依赖的 P1 项。剩余 P1（Giscus 评论需装 App+repo/category id；Umami 需 endpoint+website id；自动 OG 图较重）都需要用户提供凭据/先决操作，待用户确认后推进。

## P1 #6 子①：FAQ/HowTo JSON-LD（GEO 结构化数据，用户确认「先做①」）

- 新增 `src/utils/structured-data.ts`：纯字符串解析 `entry.body`（原始 Markdown），零依赖。导出 `extractStructuredData(body, fallbackTitle)`。
- 检测规则：
  - **FAQPage**：`:::faq` 容器内按 `**问：/Q:/Question:` 行标记拆成 Question/Answer；**以及** 任意 `:::tip`/`:::note`/`:::warning`/`:::caution`/`:::important` 块若正文含 `问：/Q:` Q&A 也转为 FAQ（覆盖「:::tip + 问答」来源）；块带 `title=` 且无内部标记时整体作为单条 FAQ。
  - **HowTo**：`:::howto{title=...}` 容器内有序列表（`1.`/`1)`/`1、`）逐项转 `HowToStep`，`name` 按首个 `：`/`:` 拆分（如「第一步：xxx」）；**以及** 标题含「步骤/教程/操作指南」的二级~四级标题 + 紧接有序列表（覆盖「步骤」来源）。
  - 文本清洗：去 `**`/`*`/`_`/`` ` ``/链接，合并空白。
- `astro.config.mjs` 的 rehypeComponents 注册 `faq`/`howto` → `AdmonitionComponent`，使内容**可见渲染**（JSON-LD 镜像可见文本，符合 schema.org 要求）。
- `variables.styl` 加 `--admonitions-color-faq`（hue 280）/ `--admonitions-color-howto`（hue 200）；`markdown-extend.styl` 加 `.bdm-faq`/`.bdm-howto` 样式（复用 tip 结构 + 问号/列表 SVG mask）。
- `[...slug].astro`：import 工具，构建 `jsonLdBlocks = [BlogPosting, FAQPage?, HowTo?]`，用 `jsonLdBlocks.map(...)` 输出多个 `<script type="application/ld+json">`。
- 验证：`markdown-showcase.md` 加 demo（2 条 FAQ + 3 步 HowTo）。`pnpm build` 通过；产物 `dist/posts/markdown-showcase/index.html` 含 FAQPage(2 Question) + HowTo(3 Step)，问题文本在页面可见；`hello-im-ink` 仅 BlogPosting（无误报）。
- 待做：P1 #6 子② 生成根 `llms.txt`（构建期聚合 frontmatter）。

## P1 #6 子③：Giscus 评论接入（用户提供完整 config）

- 用户粘贴了 Giscus `<script>` 全部属性。已落地：
  - `src/types/config.ts` 新增 `GiscusConfig` 类型（enabled/repo/repoId/category/categoryId/mapping/strict/reactionsEnabled/emitMetadata/inputPosition/lang）。
  - `src/config.ts` 新增 `giscusConfig`（enabled:true，repo=`Kiuyor/ink_blog`，repoId=`R_kgDOP7E2YA`，category=`Announcements`，categoryId=`DIC_kwDOP7E2YM4DCURO`，mapping=`pathname`，strict=`0`，reactionsEnabled=`1`，emitMetadata=`0`，inputPosition=`bottom`，lang=`zh-CN`）。
  - `src/components/Giscus.astro`：enabled 时渲染 `<div class="giscus mt-10" data-pagefind-ignore="true">` + 内联脚本动态创建 `giscus.app/client.js`（data-* 由脚本运行时注入，故静态 HTML 中搜不到 data-repo-id 属正常）。
  - `[...slug].astro` import 并放在 post-container 之后、上下篇导航之前 → **仅文章页有评论**。
- 健壮性（关键坑）：直接 `window.swup && ...` 在脚本早于 swup 初始化时会漏注册 `page:view` 钩子 → 复制 Layout.astro 的 `swup:enable` 兜底模式；并加 **URL 守卫**（`lastUrl` + 已有 iframe 则跳过）避免首屏 page:view 与初始 load 重复注入导致闪烁。主题跟随：MutationObserver 监听 `<html class="dark">` 变化 → `postMessage({giscus:{setConfig:{theme}}})` 给 iframe。
- 验证：`pnpm build` 通过；`/posts/markdown-showcase/` HTTP 200，含 `class="giscus"`×1、`giscus.app/client.js`×1、`swup:enable`×1。
- ⚠️ 部署前置：仓库 `Kiuyor/ink_blog` 须为 **public** 且已开启 **Discussions**，并在 GitHub 装好 **Giscus App**，否则评论框会报错；本地 dist 预览因无 GitHub 会话同样不显示，属正常。
- 待做（P1 仅余）：Umami 自托管统计（需 endpoint+website id）、自动 OG 图（satori/resvg，最重）。

## 后续（P1–P3 待排期执行）
- P1：Giscus 评论（需先在 `Kiuyor/ink_blog` 装 Giscus App）、llms.txt + JSON-LD（BlogPosting/FAQ/HowTo）、Umami 统计脚本、自动 OG 图。
- P2：平台分发、轻量 CMS、主题切换。
- P3：双语、性能/CWV、可迁移文档。
